{
  "name": "mma-council-website",
  "version": "1.0.0",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "description": "",
  "scripts": {
    "start": "parcel index.html css/index.css --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build index.html --no-content-hash --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel"
  },
  "keywords": [],
  "author": "Dipayan Das",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.9.3",
    "parcel": "^2.8.2"
  },
  "dependencies": {
    "@studio-freight/lenis": "^0.2.28",
    "eslint": "^8.49.0",
    "gsap": "^3.11.4",
    "imagesloaded": "^5.0.0",
    "splitting": "^1.0.6"
  }
}
